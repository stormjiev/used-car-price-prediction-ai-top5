# 环境准备日志 - v2 高分方案

**执行日期**: 2025-12-04
**系统环境**: Python 3.13.5, Apple Silicon M4 (arm64), 无GPU, 24GB内存
**执行状态**: ✅ **环境就位，可开始执行**

---

## 1. 环境检查结果

### 1.1 系统配置

| 项目 | 实际值 | 预期值 | 状态 |
|-----|-------|-------|------|
| **Python版本** | 3.13.5 | 3.13+ | ✅ 满足 |
| **系统架构** | arm64 | arm64 | ✅ 匹配 |
| **操作系统** | macOS Darwin 24.6.0 | macOS | ✅ 满足 |
| **内存** | 24GB | 16GB+ | ✅ 充足 |
| **GPU/加速** | 无CUDA，Metal可用 | CPU或GPU | ✅ 接受 |

### 1.2 必需依赖包检查

| 包名 | 版本 | 用途 | 状态 |
|-----|------|------|------|
| **pandas** | 2.2.3 | 数据处理 | ✅ 已安装 |
| **numpy** | 2.2.5 | 数值计算 | ✅ 已安装 |
| **scikit-learn** | 已安装 | 数据预处理、CV | ✅ 已安装 |
| **lightgbm** | 4.6.0 | LGB模型 | ✅ 已安装 |
| **catboost** | 1.2.8 | CatBoost模型 | ✅ 已安装 |
| **torch** | 2.9.1 | 备选NN框架 | ✅ 已安装 |
| **TensorFlow** | 2.20.0 | NN模型、Keras | ⚠️ 需特殊处理 |

---

## 2. TensorFlow 安装与适配

### 2.1 安装问题与解决过程

**问题**: Python 3.13.5与TensorFlow的兼容性

| 尝试方案 | 结果 | 备注 |
|--------|------|------|
| tensorflow-macos | ❌ 包不存在 | 已弃用，不再提供 |
| tensorflow==2.13.0 | ❌ 版本不可用 | PyPI只有2.20.0 |
| tensorflow==2.20.0 | ✅ 安装成功 | 200.7MB，完整安装 |
| 导入测试 | ⚠️ 分段故障 | arm64已知问题 |

### 2.2 TensorFlow 2.20.0 安装日志

```
Successfully installed:
- tensorflow-2.20.0 (cp313-cp313-macosx_12_0_arm64)
- keras-3.12.0
- tensorboard-2.20.0
- grpcio-1.76.0
- libclang-18.1.1
- 以及其他13个依赖包

总下载量: 200.7 MB
安装耗时: 约3分钟
```

### 2.3 兼容性状态

**当前状态**: TensorFlow 2.20.0安装完毕，但导入时存在arm64 macOS已知问题

**对应策略**:
1. **优先方案**: 使用PyTorch替代NN模块
   - PyTorch 2.9.1已安装
   - Metal Performance Shaders (MPS)可用
   - 兼容性更好，无导入问题

2. **备选方案**: 待TensorFlow问题解决后启用
   - 保持TensorFlow安装，可后续修复
   - Keras 3.12已随TensorFlow安装
   - 不影响当前执行进度

---

## 3. PyTorch环境验证

### 3.1 PyTorch配置

```python
✅ PyTorch 2.9.1 - 已安装
✅ CUDA - 不可用 (预期，无GPU)
✅ Metal Performance Shaders - 可用 (Apple Silicon加速)
```

### 3.2 可用计算资源

| 资源 | 状态 | 说明 |
|-----|------|------|
| **CPU多核** | ✅ 可用 | 8核M4芯片，多线程支持 |
| **Metal GPU加速** | ✅ 可用 | PyTorch可利用 |
| **内存** | ✅ 充足 | 24GB，足以处理150K×200特征 |
| **磁盘** | ✅ 充足 | 用于存储OOF和中间结果 |

---

## 4. 代码参考与目录结构

### 4.1 源代码位置

**原高分方案**: `参考高分方案/`
```
参考高分方案/
├── code/
│   ├── feature/
│   │   ├── generation.py          (通用预处理)
│   │   ├── Tree_generation.py     (树特征，83个)
│   │   └── NN_generation.py       (NN特征，29个)
│   ├── model/
│   │   ├── model.py               (完整管道)
│   │   ├── lgb_model.py           (LGB 10-Fold)
│   │   ├── cab_model.py           (CAB 10-Fold)
│   │   ├── nn_model.py            (NN 5-Fold)
│   │   └── stack+mix.py           (融合)
│   └── requirements.txt
└── README.md
```

### 4.2 目标项目结构

**新项目**: `/Users/macpro/Downloads/ai/day18-分析式AI基础/v2/`

```
v2/
├── plans/
│   └── 高分方案计划.md
├── code/                          ← 参考自高分方案
│   ├── feature/
│   │   ├── generation.py
│   │   ├── Tree_generation.py
│   │   └── NN_generation.py
│   ├── model/
│   │   ├── model.py
│   │   ├── lgb_model.py
│   │   ├── cab_model.py
│   │   ├── nn_model.py
│   │   └── stack+mix.py
│   └── requirements.txt
├── data/
│   ├── raw/                       ← 符号链接到统一数据源
│   │   ├── used_car_train_20200313.csv
│   │   ├── used_car_testB_20200421.csv
│   │   └── used_car_sample_submit.csv
│   └── processed/                 ← 特征缓存目录
├── results/
│   ├── submissions/               ← 最终提交文件
│   └── metrics/                   ← 评估数据
├── user_data/                     ← 模型输出临时目录
├── recoveryplan/                ← 本目录：执行日志
│   ├── 01-环境准备日志.md        (当前文件)
│   ├── 02-特征工程进度.md
│   ├── 03-模型训练进度.md
│   ├── 04-融合结果.md
│   ├── 05-参数对照表.md
│   └── 06-最终报告.md
└── README.md
```

### 4.3 数据链接

**统一数据源**: `/Users/macpro/Downloads/ai/day18-分析式AI基础/Case-二手车价格预测-data/`

**创建符号链接** (命令):
```bash
cd /Users/macpro/Downloads/ai/day18-分析式AI基础/v2/data
ln -s ../../Case-二手车价格预测-data raw
```

---

## 5. 完整执行计划 (可追溯)

### Phase 0: 环境验证 (当前，0分钟)
- [x] Python 3.13.5环境确认
- [x] 系统架构arm64确认
- [x] 必需依赖包检查
- [x] TensorFlow安装与兼容性评估
- [x] PyTorch备选方案验证

**状态**: ✅ 完成
**预期结果**: 环境就位，无阻塞因素

---

### Phase 1: 项目初始化 (预计30分钟)

#### 1.1 创建目录结构
**步骤**:
1. 确保 v2 目录存在
2. 创建 code/, data/processed/, results/, user_data/, recoveryplan/ 子目录
3. 验证所有目录权限正确

**预期结果**:
- ✅ 所有目录已创建
- ✅ 权限可写 (755)
- ✅ 约300B磁盘使用

**验证命令**:
```bash
ls -la /Users/macpro/Downloads/ai/day18-分析式AI基础/v2/
find ./v2 -type d | wc -l
```

---

#### 1.2 参考源代码
**步骤**:
1. 从 参考高分方案/code/ 参考至 v2/code/
2. 保持目录结构完整 (feature/, model/)
3. 验证所有Python文件完整

**预期结果**:
- ✅ feature/ 目录包含: generation.py, Tree_generation.py, NN_generation.py
- ✅ model/ 目录包含: model.py, lgb_model.py, cab_model.py, nn_model.py, stack+mix.py
- ✅ requirements.txt 已迁移
- ✅ 文件总数: 8+ Python文件, 1 requirements.txt

**验证命令**:
```bash
ls -la /Users/macpro/Downloads/ai/day18-分析式AI基础/v2/code/
find ./v2/code -name "*.py" | wc -l
```

---

#### 1.3 创建数据符号链接
**步骤**:
1. 创建 data/raw/ 目录
2. 创建符号链接指向统一数据源
3. 验证链接有效性

**预期结果**:
- ✅ data/raw/ 目录符号链接建立
- ✅ 可访问 used_car_train_20200313.csv (150K+行)
- ✅ 可访问 used_car_testB_20200421.csv (50K+行)
- ✅ 可访问 used_car_sample_submit.csv (样本)

**验证命令**:
```bash
ls -la /Users/macpro/Downloads/ai/day18-分析式AI基础/v2/data/raw/
wc -l /Users/macpro/Downloads/ai/day18-分析式AI基础/v2/data/raw/used_car_train_20200313.csv
```

---

#### 1.4 调整文件路径
**步骤**:
1. 打开 code/ 目录中所有 .py 文件
2. 搜索数据路径硬编码 (如原项目中的绝对路径)
3. 替换为相对路径或使用 v2 作为基准

**预期结果**:
- ✅ 所有数据加载使用 v2/data/raw/ 路径
- ✅ 所有输出保存到 v2/user_data/ 或 v2/results/
- ✅ 代码可在新位置正常运行

**影响的文件**:
- generation.py (数据加载路径)
- Tree_generation.py (输入输出路径)
- NN_generation.py (输入输出路径)
- lgb_model.py (OOF保存路径)
- cab_model.py (OOF保存路径)
- nn_model.py (OOF保存路径)
- stack+mix.py (OOF加载、预测保存路径)
- model.py (管道集成的路径)

---

### Phase 2: 数据预处理与特征工程 (预计1小时15分钟)

#### 2.1 数据预处理 (预计30分钟)
**执行**:
```bash
cd /Users/macpro/Downloads/ai/day18-分析式AI基础/v2/code/feature
python generation.py
```

**预期结果**:
| 指标 | 预期值 | 实际值 | 差异 |
|-----|-------|--------|------|
| 输出文件 | user_data/combined_preprocessed.csv | ? | |
| 训练样本数 | 150000 | ? | |
| 测试样本数 | 50000 | ? | |
| 特征列数 | 29-31 (含source, price) | ? | |
| 缺失值 | 0 (NaN) | ? | |
| 耗时 | <30分钟 | ? | |

**验证命令**:
```bash
cd /Users/macpro/Downloads/ai/day18-分析式AI基础/v2
python -c "
import pandas as pd
df = pd.read_csv('user_data/combined_preprocessed.csv')
print(f'Shape: {df.shape}')
print(f'NaN count: {df.isnull().sum().sum()}')
print(f'Columns: {list(df.columns[:10])}...')
"
```

---

#### 2.2 树特征工程 (预计15分钟)
**执行**:
```bash
cd /Users/macpro/Downloads/ai/day18-分析式AI基础/v2/code/feature
python Tree_generation.py
```

**预期结果**:
| 指标 | 预期值 | 实际值 | 差异 |
|-----|-------|--------|------|
| user_data/train_tree.csv | 150000 行 × 83 列 | ? | |
| user_data/test_tree.csv | 50000 行 × 83 列 | ? | |
| 特征完整性 | 无NaN值 | ? | |
| 耗时 | <15分钟 | ? | |

**验证命令**:
```bash
cd /Users/macpro/Downloads/ai/day18-分析式AI基础/v2
python -c "
import pandas as pd
df_train = pd.read_csv('user_data/train_tree.csv')
df_test = pd.read_csv('user_data/test_tree.csv')
print(f'Train: {df_train.shape}, NaN: {df_train.isnull().sum().sum()}')
print(f'Test: {df_test.shape}, NaN: {df_test.isnull().sum().sum()}')
"
```

---

#### 2.3 NN特征工程 (预计3分钟)
**执行**:
```bash
cd /Users/macpro/Downloads/ai/day18-分析式AI基础/v2/code/feature
python NN_generation.py
```

**预期结果**:
| 指标 | 预期值 | 实际值 | 差异 |
|-----|-------|--------|------|
| user_data/train_nn.csv | 150000 行 × 29 列 | ? | |
| user_data/test_nn.csv | 50000 行 × 29 列 | ? | |
| 数值范围 | [0, 1] (MinMax) | ? | |
| 耗时 | <3分钟 | ? | |

**验证命令**:
```bash
cd /Users/macpro/Downloads/ai/day18-分析式AI基础/v2
python -c "
import pandas as pd
df_train = pd.read_csv('user_data/train_nn.csv')
print(f'Min: {df_train.min().min()}, Max: {df_train.max().max()}')
print(f'Shape: {df_train.shape}')
"
```

---

### Phase 3: 基础模型训练 (预计2.5小时，可并行)

#### 3.1 LightGBM 10-Fold CV (预计40分钟)
**执行**:
```bash
cd /Users/macpro/Downloads/ai/day18-分析式AI基础/v2/code/model
python lgb_model.py
```

**预期结果**:
| 指标 | 预期值 | 实际值 | 差异 |
|-----|-------|--------|------|
| user_data/lgb_train.csv | OOF预测, 150K行 | ? | |
| user_data/lgb_test.csv | 测试预测, 50K行 | ? | |
| CV MAE | 420-430 | ? | |
| 耗时 | <40分钟 | ? | |

**验证命令**:
```bash
cd /Users/macpro/Downloads/ai/day18-分析式AI基础/v2
python -c "
import pandas as pd
from sklearn.metrics import mean_absolute_error
df_train = pd.read_csv('user_data/lgb_train.csv')
# 计算CV MAE (假设第一列是预测，第二列是真实值)
mae = mean_absolute_error(df_train.iloc[:, 1], df_train.iloc[:, 0])
print(f'LGB CV MAE: {mae:.2f}')
"
```

---

#### 3.2 CatBoost 10-Fold CV (预计50分钟)
**执行**:
```bash
cd /Users/macpro/Downloads/ai/day18-分析式AI基础/v2/code/model
python cab_model.py
```

**预期结果**:
| 指标 | 预期值 | 实际值 | 差异 |
|-----|-------|--------|------|
| user_data/cab_train.csv | OOF预测, 150K行 | ? | |
| user_data/cab_test.csv | 测试预测, 50K行 | ? | |
| CV MAE | 420-430 | ? | |
| 耗时 | <50分钟 | ? | |

---

#### 3.3 Neural Network 5-Fold CV (预计45分钟)
**执行**:
```bash
cd /Users/macpro/Downloads/ai/day18-分析式AI基础/v2/code/model
python nn_model.py  # 或改为 PyTorch 版本
```

**预期结果**:
| 指标 | 预期值 | 实际值 | 差异 |
|-----|-------|--------|------|
| user_data/nn_train.csv | OOF预测, 150K行 | ? | |
| user_data/nn_test.csv | 测试预测, 50K行 | ? | |
| CV MAE | 420-430 | ? | |
| 耗时 | <45分钟 | ? | |

**备注**: 如TensorFlow导入失败，用PyTorch替代实现

---

### Phase 4: 融合与最终提交 (预计15分钟)

#### 4.1 Stacking + Blending (预计15分钟)
**执行**:
```bash
cd /Users/macpro/Downloads/ai/day18-分析式AI基础/v2/code/model
python stack+mix.py
```

**预期结果**:
| 指标 | 预期值 | 实际值 | 差异 |
|-----|-------|--------|------|
| results/submissions/predictions.csv | 50K行 × 2列 (SaleID, price) | ? | |
| 融合MAE | 404-410 (线下估计) | ? | |
| 耗时 | <15分钟 | ? | |

**验证命令**:
```bash
cd /Users/macpro/Downloads/ai/day18-分析式AI基础/v2
python -c "
import pandas as pd
df = pd.read_csv('results/submissions/predictions.csv')
print(f'Shape: {df.shape}')
print(f'Columns: {list(df.columns)}')
print(f'Price range: [{df[\"price\"].min():.2f}, {df[\"price\"].max():.2f}]')
"
```

---

### Phase 5: 文档与验证 (预计30分钟)

#### 5.1 完成执行日志
**生成文件**:
- 02-特征工程进度.md (特征数据统计)
- 03-模型训练进度.md (各模型CV分数)
- 04-融合结果.md (最终MAE)
- 05-参数对照表.md (与高分方案对比)
- 06-最终报告.md (成功确认)

**预期结果**:
- ✅ 所有执行步骤记录清楚
- ✅ 所有关键指标已验证
- ✅ 任何异常/偏差已说明
- ✅ 可性确认

---

## 6. 总体执行计划时间表

| 阶段 | 任务 | 预计时间 | 状态 |
|-----|------|---------|------|
| Phase 0 | 环境准备 | 0分钟 | ✅ 完成 |
| Phase 1 | 项目初始化 | 30分钟 | 待执行 |
| Phase 2.1 | 数据预处理 | 30分钟 | 待执行 |
| Phase 2.2 | 树特征工程 | 15分钟 | 待执行 |
| Phase 2.3 | NN特征工程 | 3分钟 | 待执行 |
| Phase 3.1 | LGB训练 | 40分钟 | 待执行 |
| Phase 3.2 | CAB训练 | 50分钟 | 待执行 |
| Phase 3.3 | NN训练 | 45分钟 | 待执行 |
| Phase 4 | 融合提交 | 15分钟 | 待执行 |
| Phase 5 | 文档验证 | 30分钟 | 待执行 |
| **总计** | **完整** | **~3.5小时** | |

---

## 7. 风险与应对

### 7.1 已识别风险

| 风险 | 概率 | 影响 | 应对方案 |
|-----|------|------|---------|
| **TensorFlow导入失败** | 中 | 高 | 用PyTorch替代NN模块 |
| **特征数据过大** | 低 | 中 | 增加内存分配，分批处理 |
| **路径不兼容** | 低 | 高 | 使用相对路径，环境变量 |
| **OOF维度不匹配** | 低 | 高 | 检查CV折分逻辑，添加验证 |
| **融合权重不优** | 中 | 低 | 调整0.5/0.5权重组合 |

### 7.2 应对步骤

**如遇到错误**:
1. 记录完整错误信息到 recoveryplan/ 目录
2. 根据风险表查找应对方案
3. 执行调整，重新运行该阶段
4. 更新进度文档，记录修改原因

---

## 8. 成功标志

✅ **Phase 1 完成**:
- v2 完整目录结构就位
- 所有代码文件已迁移
- 数据链接已建立
- 路径已调整

✅ **Phase 2 完成**:
- combined_preprocessed.csv 150K × 29-31列，无NaN
- train_tree.csv 150K × 83列，无NaN
- train_nn.csv 150K × 29列，值在[0,1]范围

✅ **Phase 3 完成**:
- LGB CV MAE: 420-430
- CAB CV MAE: 420-430
- NN CV MAE: 420-430

✅ **Phase 4 完成**:
- Stacking MAE: 410-420
- 最终融合MAE: 404-415 (线下估计)
- predictions.csv 50K × 2列已生成

✅ **Phase 5 完成**:
- 所有执行日志已记录
- 参数对比表已验证
- 最终报告已完成

---

## 9. 下一步行动

### 立即执行 (按顺序)

**[ ] 步骤1: Phase 1 - 项目初始化 (30分钟)**
```bash
# 1.1 创建目录
mkdir -p v2/data/processed
mkdir -p v2/results/submissions
mkdir -p v2/user_data

# 1.2 参考代码 (需手工或脚本)
# 1.3 创建数据链接 (需手工或脚本)
# 1.4 调整路径 (需编辑代码文件)
```

**[ ] 步骤2: Phase 2 - 特征工程 (48分钟)**
```bash
cd v2/code/feature
python generation.py
python Tree_generation.py
python NN_generation.py
```

**[ ] 步骤3: Phase 3 - 模型训练 (2小时15分钟)**
```bash
cd v2/code/model
python lgb_model.py  &  # 后台并行
python cab_model.py  &
python nn_model.py   &
wait  # 等待所有任务完成
```

**[ ] 步骤4: Phase 4 - 融合提交 (15分钟)**
```bash
python stack+mix.py
```

**[ ] 步骤5: Phase 5 - 完成文档 (30分钟)**
```bash
# 编写 recoveryplan/ 中的进度文档
```

---

## 10. 文档更新频率

**实时更新** (执行中):
- 每个Phase完成后，更新对应的进度文件
- 记录实际耗时与预期偏差
- 记录任何运行错误与解决方案

**最终总结** (全部完成后):
- 生成 06-最终报告.md，包含:
  - 完整执行时间
  - 各阶段实际MAE vs 预期
  - 问题与解决方案总结
  - 高分方案确认

---

**准备就绪，等待执行命令** 🚀

